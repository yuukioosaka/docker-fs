<?xml version="1.0"?>
<!--
  Global FreeSWITCH variables. Values come from the environment so the
  same image works in any deployment.
-->
<include>
  <X-PRE-PROCESS cmd="set" data="hostname=${FS_HOSTNAME}"/>
  <X-PRE-PROCESS cmd="set" data="default_password=${FS_DEFAULT_PASSWORD}"/>
  <X-PRE-PROCESS cmd="set" data="default_provider=example.com"/>
  <X-PRE-PROCESS cmd="set" data="default_provider_username=username"/>
  <X-PRE-PROCESS cmd="set" data="default_provider_password=password"/>
  <X-PRE-PROCESS cmd="set" data="hold_music=silence_stream://-1"/>
  <X-PRE-PROCESS cmd="set" data="local_ip_v4=${FS_LOCAL_IP}"/>
  <X-PRE-PROCESS cmd="set" data="domain=${FS_DOMAIN}"/>
  <X-PRE-PROCESS cmd="set" data="external_rtp_ip=$${local_ip_v4}"/>
  <X-PRE-PROCESS cmd="set" data="external_sip_ip=$${local_ip_v4}"/>

  <!-- PostgreSQL DSN, consumed by mod_pgsql / the core database section -->
  <X-PRE-PROCESS cmd="set" data="pgsql_dsn=pgsql://${PGUSER}:${PGPASSWORD}@${PGHOST}:${PGPORT}/${PGDATABASE}"/>
</include>
